<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Page Design</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
        }

        .container {
            
            max-width: 1000px;
            margin: 0 auto;
            border-radius: 8px;
        }

        /* Book gallery layout */
        .book-gallery {
            display: flex;
            margin-bottom: 20px;
        }

        .main-image img {
            width: 250px;
            border-radius: 8px;
            margin-bottom: 10px;
        }

        .thumbnails {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 10px;
            margin-right: 30px;
        }

        .thumbnails img {
            width: 60px;
            border: 2px solid #ddd;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
        }

        .thumbnails img:hover {
            border-color: #000;
        }

        .book-info {
            margin-top: 5px;
            margin-left: 40px;
        }

        h1 {
            font-size: 28px;
            color: #585757;
        }

        h2 {
            font-size: 18px;
            color: #e20808;
            margin-bottom: 15px;
        }
        h3 {
            font-size: 18px;
            color: #2e2e2e;
            margin-bottom: 12px;
        }
        h4 {
            font-size: 15px;
            color: #737272;
            margin-bottom: 10px;
        }

        .rating {
            display: flex;
            margin-bottom: 15px;
        }
        .ratingavarage {
            display: flex;
            margin-bottom: 15px;
        }

        .star {
            font-size: 24px;
            color: #ccc;
            cursor: pointer;
        }

        .gold {
            color: #ffcc00;
        }

        .summary {
            margin-bottom: 20px;
            font-size: 16px;
            color: #555;
        }

        .buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
        }

        button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

        .add-to-cart {
            background-color: #f0f0f0;
            color: #333;
        }

        .buy-now {
            background-color: #28a745;
            color: white;
        }

        .edition-info {
            margin-top: 20px;
            font-size: 16px;
            color: #555;
        }

        ul {
            list-style-type: none;
            margin-top: 10px;
        }

        .rating-section {
            margin-top: 30px;
        }

        .user-rating {
            display: flex;
            gap: 5px;
            margin-bottom: 15px;
        }

        .user-rating .star {
            font-size: 24px;
            color: #ccc;
            cursor: pointer;
        }

        .submit-rating {
            background-color: #007bff;
            color: white;
        }

        /* Zoom and Magnifier Styles */
        .zoom {
            position: relative;
            display: inline-block;
        }

        .original {
            cursor: crosshair;
            position: relative;
        }

        .zoom .viewer {
            position: absolute;
            top: 0;
            left: 100%;
            width: 400px; /* Width of the zoomed image */
            height: 500px; /* Height of the zoomed image */
            overflow: hidden;
            display: none; /* Hidden initially */
            border: 1px solid #ddd;
        }

        .zoom .viewer img {
            position: absolute;
            width: 200%; /* Increased width for better zoom */
            height: auto;
        }

        .magnifier {
            position: absolute;
            background: rgba(255, 255, 255, 0.4);
            border: 1px solid black;
            width: 120px; /* Updated magnifier size */
            height: 120px;
            display: none; /* Hidden initially */
            pointer-events: none;
        }

        .original:hover + .magnifier,
        .original:hover + .viewer {
            display: block;
        }

        .reviews-section {
    display: flex;
    gap: 50px;
    background-color: #fff;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
}

.rating-form {
    width: 300px;
}

.rating-form h3 {
    margin-bottom: 15px;
    font-size: 18px;
}

.rating-form p {
    margin-bottom: 10px;
    font-size: 16px;
}

.stars span {
    font-size: 24px;
    cursor: pointer;
    color: #ccc;
}

.stars span:hover, 
.stars span:hover ~ span {
    color: #f0b825;
}

textarea {
    width: 100%;
    height: 100px;
    margin: 15px 0;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    resize: none;
}

.submit-btn {
    background-color: #f4d005;
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;
}

.submit-btn:hover {
    background-color: #e09a00;
}

.community-reviews {
    width: 400px;
}

.community-reviews h3 {
    margin-bottom: 15px;
    font-size: 18px;
} 

.review-placeholder {
    display: flex;
    align-items: center;
    margin-bottom: 20px;
}

.avatar {
    width: 40px;
    height: 40px;
    background-color: #ccc;
    border-radius: 50%;
    margin-right: 10px;
}

.review-lines {
    flex-grow: 1;
}

.line {
    width: 100%;
    background-color: #f9f7f7;
    margin-bottom: 5px;
    padding: 1px 10px;
    border-radius: 10px;
}

.line p {
    font-size: small;
    font-weight: 500;
    font-family: 'Poppins', sans-serif;

}

.line:last-child {
    width: 95%;
}

  .related-products-section {
    margin: 20px 0;
    text-align: center;
  }

  .slider-container {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    width: 100%;
  }

  .book-container {
    display: flex;
    overflow-x: scroll;
    scroll-behavior: smooth;
    padding: 10px 0;
    white-space: nowrap;
    width: 100%;
    scrollbar-width: none; 
    -ms-overflow-style: none; 
  }

  .book-container::-webkit-scrollbar {
    display: none;
  }
  .book-card {
    display: inline-block;
    min-width: 200px;
    margin: 0 10px;
    border: 1px solid #ddd;
    padding: 10px;
    background-color: #fff;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    text-align: left;
  }

  .book-image {
    width: 100%;
    height: auto;
  }

  .book-title {
    font-size: 18px;
    font-weight: bold;
    margin-top: 10px;
  }

  .book-author {
    font-size: 14px;
    color: #555;
    margin-top: 5px;
  }

  .rating {
    color: #f39c12;
    margin-top: 5px;
  }

  .price {
    font-size: 16px;
    margin-top: 10px;
  }

  .current-price {
    color: #e74c3c;
    font-weight: bold;
  }

  .original-price {
    text-decoration: line-through;
    color: #999;
    margin-left: 5px;
  }

  .discount {
    color: #27ae60;
    font-weight: bold;
    margin-top: 5px;
  }

  .free-delivery {
    font-size: 12px;
    color: #2ecc71;
    margin-top: 10px;
  }

  .separator-line {
    margin: 10px 0;
    border: none;
    border-top: 1px solid #eee;
  }

  /* Slider Buttons */
  .slider-btn {
    background-color: #f1f1f1;
    border: none;
    font-size: 18px;
    padding: 10px;
    cursor: pointer;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 1;
  }

  .prev-btn {
    left: 10px;
  }

  .next-btn {
    right: 10px;
  }

  .slider-btn:hover {
    background-color: #ddd;
  }
</style>


    </style>
</head>
<body>
    <%- include('../partials/userHeader') %>
    <%- include('../partials/userSidebar') %>

    <div class="container">
        <div class="container">
            <%- include('../partials/breadcrumb', { breadcrumbs: breadcrumbs }) %> 
           
          </div>
          
        <div class="book-gallery">
            <!-- Thumbnail Images -->
            <div class="thumbnails">
                <img src="/images/uploads/<%= book.bookDetails.coverImageName || '' %>" alt="Book Image 1" onclick="changeImage('/images/uploads/<%= book.bookDetails.coverImageName %>')">
                <img src="/images/uploads/<%= book.bookDetails.backImageName  || '' %>" alt="Book Image 2" onclick="changeImage('/images/uploads/<%= book.bookDetails.backImageName %>')">
                <img src="/images/uploads/<%= book.bookDetails.contentImageName || '' %> " alt="Book Image 3" onclick="changeImage('/images/uploads/<%= book.bookDetails.contentImageName %>')">
            </div>
            <!-- Main Display Image -->
            <div class="main-image">
                <div class="zoom">
                    <!-- Original Image -->
                    <div class="original book-cover">
                        <img id="main-book-image" src="/images/uploads/<%= book.bookDetails.coverImageName %>" alt="Main Image">
                    </div>
                    <!-- Zoomed Viewer -->
                    <div class="viewer">
                        <img id="zoomed-book-image" src="/images/uploads/<%= book.bookDetails.coverImageName %>" alt="Zoomed Image">
                    </div>
                    <!-- Magnifier Effect -->
                    <div class="magnifier"></div>
                </div>
            </div>

            <div class="book-info">
                <h1><%= book.bookDetails.book_name || '' %></h1>
                <h4><%= book.bookDetails.author_name || '' %></h4>
                <h3>₹<%= book.bookDetails.price || '' %>/-</h3>
                <h4 style="color: #28a745;">20% off</h4>
                    <% if (book.bookDetails.stock > 0) { %>
                        <h3> <%= book.bookDetails.stock %> in stock </h3> 
                    <% } else { %>
                        <h1>solid Out</h1>
                     <h2>This item currently Out of Stock</h2>
                    <% } %>
                                   
                <div class="ratingavarage" stars="<%= book.average_rating || 0 %>">
                    <span class="star">★</span>
                    <span class="star">★</span>
                    <span class="star">★</span>
                    <span class="star">★</span>
                    <span class="star">★</span>
                </div>
               
                <div class="buttons">
                    <button class="add-to-cart">Add to cart</button>
                    <button class="buy-now">Buy now</button>
                </div>
            </div>
        </div>

        <p class="summary">
            <%= book.bookDetails.about_book || '' %>
        </p>
        <div class="edition-info">
            <p><strong>This edition</strong></p>
            <ul>
                <li><%= book.bookDetails.pages || 'N/A' %> pages, Hardcover</li>
                <li><%= book.bookDetails.published_date || 'Unknown' %> by <%= book.bookDetails.publisher_name || 'Unknown Publisher' %></li>
                <li><%= book.bookDetails.language || 'Unknown' %></li>
            </ul>
        </div>

       
            <div class="related-products-section">
                <h2>Related Products</h2>
                
                <!-- Slider Container -->
                <div class="slider-container">
                  <button class="slider-btn prev-btn" onclick="slideLeft()">&#10094;</button> <!-- Left arrow -->
                  
                  <div class="book-container" id="related-books-slider">
                    <% relatedBooks.slice(0, 10).forEach(function(book) { %> <!-- Adjust number of books -->
                      <div class="book-card" data-id="<%= book._id %>">
                        <img src="/images/uploads/<%= book.bookDetails.coverImageName %>" alt="Book Cover" class="book-image">
                        <div class="book-info">
                          <div class="book-title"><%= book.bookDetails.book_name || '' %></div>
                          <div class="book-author">by <%= book.bookDetails.author_name || '' %></div>
                          <div class="rating"><%= book.average_rating || 'N/A' %>★</div>
                          <div class="price">
                            <span class="current-price">₹<%= book.bookDetails.price || '' %></span>
                            <span class="original-price">₹<%= book.bookDetails.original_price || '' %></span>
                          </div>
                          <div class="discount"><%= book.bookDetails.discount || '' %> off</div>
                          <div class="free-delivery">Free Delivery</div>
                        </div>
                        <hr class="separator-line">
                      </div>
                    <% }); %>
                  </div>
                  
                  <button class="slider-btn next-btn" onclick="slideRight()">&#10095;</button> <!-- Right arrow -->
                </div>
              
          
       
        <div class="reviews-section">
            <!-- Ratings and Review Form -->
           
            <div class="rating-form">
                <div class="rating-section">
                    <h3>Ratings & Reviews</h3>
                    <p>What do you think?</p>
                    <div class="rating">
                        <span id="star-1" onclick="addRating(1)" class="star">☆</span>
                        <span id="star-2" onclick="addRating(2)" class="star">☆</span>
                        <span id="star-3" onclick="addRating(3)" class="star">☆</span>
                        <span id="star-4" onclick="addRating(4)" class="star">☆</span>
                        <span id="star-5" onclick="addRating(5)" class="star">☆</span>
                    </div>
                    <textarea id="review" placeholder="Write a review" required></textarea>
                    <button class="submit-btn" data-book-id="<%= book._id %>">Submit</button>
                </div>
            </div>
    
            <!-- Community Reviews -->
            <div class="community-reviews">
                <h3>Community Reviews</h3>
                <div class="review-placeholder">
                    <div class="review-lines">
                        <% reviews.forEach((review)=>{   %>   
                        <div class="line">
                            <p><%= review.userName %></p> 
                            <div class="ratingavarage" stars="<%= review.rating %>"> 
                                <span class="star">★</span>
                                <span class="star">★</span>
                                <span class="star">★</span>
                                <span class="star">★</span>
                                <span class="star">★</span>
                            </div>
                            <p><%= review.review %></p></div>
                        <% }) %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    




    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script>
       let stars = 0;

function addRating(n) {
    for (let i = 1; i <= 5; i++) {
        document.getElementById(`star-${i}`).classList.remove('gold');
        document.getElementById(`star-${i}`).innerHTML = '☆'; 
    }
    for (let i = 1; i <= n; i++) {
        document.getElementById(`star-${i}`).classList.add('gold');
        document.getElementById(`star-${i}`).innerHTML = '★'; 
    }
    stars = n; 
}

// Add click event listener for the submit button
document.querySelector('.submit-btn').addEventListener('click', function() {
    const bookId = this.getAttribute('data-book-id');
    const review = document.getElementById('review').value;

    fetch(`/userbook/rating`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({ stars, bookId, review })
    })
    .then(response => response.json())
    .then(data => {
        console.log('Rating submitted:', data);
        alert("Your rating has been successfully submitted!");
    })
    .catch(error => {
        console.error('Error submitting rating:', error);
    });
});

document.addEventListener("DOMContentLoaded", () => {
    let ratingElements = document.querySelectorAll('.ratingavarage');
    ratingElements.forEach(ratingElement =>{
        let stars = parseFloat(ratingElement.getAttribute('stars')); 
    console.log(stars);
    
    let starSpans = ratingElement.getElementsByClassName('star'); 
    
    for (let i = 0; i < starSpans.length; i++) {
        if (i < stars) {
            starSpans[i].classList.add('gold');
        } else {
            starSpans[i].classList.remove('gold'); 
        }
    }
    })
});



       
        function changeImage(imageSrc) {
            document.getElementById('main-book-image').src = imageSrc;
            document.getElementById('zoomed-book-image').src = imageSrc;
        }

        $(document).ready(function() {
            $('.original').mousemove(function(event) {
                var magnifier = $(this).next('.magnifier');
                var viewer = $(this).next('.viewer').find('img');

                var offset = $(this).offset();
                var x = event.pageX - offset.left;
                var y = event.pageY - offset.top;

                var posX = x / $(this).width() * 100;
                var posY = y / $(this).height() * 100;

                // Move the zoomed image
                viewer.css({
                    'transform-origin': `${posX}% ${posY}%`,
                    'transform': 'scale(2)' // Adjusted the zoom scale for better effect
                });

                // Position the magnifier on the image
                magnifier.css({
                    'top': y - magnifier.height() / 2,
                    'left': x - magnifier.width() / 2,
                    'display': 'block'
                });

                // Show viewer when mouse is over the image
                $(this).next('.viewer').show();
            });

            $('.original').mouseleave(function() {
                $(this).next('.magnifier').hide();
                $(this).next('.viewer').hide();
            });
        });


        const slider = document.getElementById('related-books-slider');

function slideLeft() {
  slider.scrollBy({
    top: 0,
    left: -slider.offsetWidth,
    behavior: 'smooth'
  });
}

function slideRight() {
  slider.scrollBy({
    top: 0,
    left: slider.offsetWidth,
    behavior: 'smooth'
  });
}
    </script>
</body>
</html>
